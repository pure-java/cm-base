server:
  address:
    nacos:
      server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:20000
    datasource:
      server-addr: localhost:3306
      username: root
      password: admin
    redis:
      server-addr: localhost
      port: 6379

pure:
  log:
    dir: ../
    level: debug
